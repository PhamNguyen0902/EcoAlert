# Expo Go on a physical device: use the LAN-reachable API Gateway address.
# Replace <LAN_IP> with the development machine's current LAN IP; do not commit .env.
EXPO_PUBLIC_API_URL=http://<LAN_IP>:3000/api
