import React, { useMemo, useState } from "react";
import { ActivityIndicator, Modal, Platform, ScrollView, StyleSheet, Text, TouchableOpacity, View } from "react-native";
import MapView, { Callout, Circle, Heatmap, Marker } from "react-native-maps";
import { AlertTriangle, Check, Filter, Layers3, MapPin, RefreshCw, X } from "lucide-react-native";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import type { IncidentDensityPoint } from "../../types";
import { useCategories } from "../../hooks/useAlerts";
import { useIncidentDensity, useIncidentDensityDrilldown } from "../../hooks/useGis";
import { Badge } from "../../components/ui/Badge";
import { useLanguage } from "../../context/LanguageContext";
import { useTheme } from "../../context/ThemeContext";
import { SEVERITY_COLORS, STATUS_COLORS } from "../../utils/constants";

type MapMode = "HEATMAP" | "POINTS" | "COMBINED";
type TimeRange = "ALL" | "7D" | "30D";
const STATUS_OPTIONS = ["ALL", "ACTIVE", "PENDING", "VERIFIED", "ASSIGNED", "IN_PROGRESS", "RESOLVED", "CLOSED"];
const SEVERITY_OPTIONS = ["ALL", "LOW", "MEDIUM", "HIGH", "CRITICAL"];

const dateFromRange = (range: TimeRange) => {
  if (range === "ALL") return undefined;
  const date = new Date();
  date.setDate(date.getDate() - (range === "7D" ? 7 : 30));
  return date.toISOString();
};

export const AdminGisScreen: React.FC<{ navigation: any }> = ({ navigation }) => {
  const insets = useSafeAreaInsets();
  const { colors, isDark } = useTheme();
  const { language } = useLanguage();
  const [mode, setMode] = useState<MapMode>("COMBINED");
  const [filterOpen, setFilterOpen] = useState(false);
  const [status, setStatus] = useState("ALL");
  const [severity, setSeverity] = useState("ALL");
  const [category, setCategory] = useState("ALL");
  const [range, setRange] = useState<TimeRange>("ALL");
  const [selected, setSelected] = useState<IncidentDensityPoint | null>(null);
  const [drilldownCenter, setDrilldownCenter] = useState<{ latitude: number; longitude: number } | null>(null);
  const categories = useCategories();
  const filters = useMemo<Record<string, string>>(() => ({
    ...(status !== "ALL" ? { status: status.toLowerCase() } : {}),
    ...(severity !== "ALL" ? { severity: severity.toLowerCase() } : {}),
    ...(category !== "ALL" ? { category } : {}),
    ...(dateFromRange(range) ? { from: dateFromRange(range)! } : {}),
  }), [category, range, severity, status]);
  const density = useIncidentDensity(filters);
  const drilldown = useIncidentDensityDrilldown(drilldownCenter, filters);
  const points = density.data?.points ?? [];
  const initialRegion = useMemo(() => {
    const seed = points[0];
    return { latitude: seed?.lat ?? 10.762622, longitude: seed?.lng ?? 106.660172, latitudeDelta: 0.09, longitudeDelta: 0.09 };
  }, [points]);
  const copy = language === "vi"
    ? { title: "Mật độ sự cố", subtitle: "Dữ liệu GIS thực theo bộ lọc", heatmap: "Mật độ", points: "Điểm sự cố", combined: "Kết hợp", filter: "Bộ lọc", filters: "Bộ lọc dữ liệu", status: "Trạng thái", severity: "Mức độ", category: "Danh mục", date: "Khoảng thời gian", all: "Tất cả", recent7: "7 ngày", recent30: "30 ngày", clear: "Xóa bộ lọc", apply: "Áp dụng", total: "Tổng", open: "Đang mở", resolved: "Đã xử lý", closed: "Đã đóng", noPoints: "Không có vị trí sự cố phù hợp", location: "Vị trí", view: "Xem chi tiết", nearby: "Sự cố lân cận", loading: "Đang tải dữ liệu GIS…", tap: "Chạm bản đồ hoặc điểm để xem các sự cố lân cận", unsupported: "Mật độ được hiển thị bằng các vùng cường độ trên thiết bị này" }
    : { title: "Incident density", subtitle: "Real GIS data using these filters", heatmap: "Density", points: "Incident points", combined: "Combined", filter: "Filter", filters: "Data filters", status: "Status", severity: "Severity", category: "Category", date: "Date range", all: "All", recent7: "Last 7 days", recent30: "Last 30 days", clear: "Clear filters", apply: "Apply", total: "Total", open: "Open", resolved: "Resolved", closed: "Closed", noPoints: "No incident locations match these filters", location: "Location", view: "View details", nearby: "Nearby incidents", loading: "Loading GIS data…", tap: "Tap the map or a point to inspect nearby incidents", unsupported: "Density is shown as intensity areas on this device" };
  const modeOptions: Array<{ id: MapMode; label: string }> = [{ id: "HEATMAP", label: copy.heatmap }, { id: "POINTS", label: copy.points }, { id: "COMBINED", label: copy.combined }];
  const showDensity = mode !== "POINTS";
  const showPoints = mode !== "HEATMAP";
  const selectPoint = (point: IncidentDensityPoint) => {
    setSelected(point);
    setDrilldownCenter({ latitude: point.lat, longitude: point.lng });
  };
  const resetFilters = () => { setStatus("ALL"); setSeverity("ALL"); setCategory("ALL"); setRange("ALL"); };
  const optionLabel = (value: string) => value === "ALL" ? copy.all : value === "ACTIVE" ? copy.open : value.replaceAll("_", " ");
  const choose = (value: string, selectedValue: string, onChoose: (next: string) => void) => <TouchableOpacity key={value} onPress={() => onChoose(value)} accessibilityRole="button" accessibilityState={{ selected: selectedValue === value }} style={[styles.option, { borderColor: selectedValue === value ? "#7C3AED" : colors.border, backgroundColor: selectedValue === value ? (isDark ? "rgba(124,58,237,0.30)" : "#F3E8FF") : colors.surface }]}><Text style={{ color: selectedValue === value ? "#7C3AED" : colors.textMuted, fontWeight: "800", fontSize: 12 }}>{optionLabel(value)}</Text>{selectedValue === value ? <Check size={15} color="#7C3AED" /> : null}</TouchableOpacity>;
  const severityColor = (item: IncidentDensityPoint) => SEVERITY_COLORS[(item.severity || "low") as keyof typeof SEVERITY_COLORS]?.text || colors.primary;
  const statusColor = (item: IncidentDensityPoint) => STATUS_COLORS[(item.status || "PENDING").toUpperCase()]?.bg || colors.primaryLight;

  return <View style={[styles.container, { backgroundColor: colors.background, paddingTop: insets.top }]}>
    <View style={[styles.header, { backgroundColor: colors.surface, borderBottomColor: colors.border }]}><View style={styles.headerCopy}><Text style={[styles.title, { color: colors.text }]}>{copy.title}</Text><Text style={[styles.subtitle, { color: colors.textMuted }]}>{copy.subtitle}</Text></View><TouchableOpacity onPress={() => density.refetch()} disabled={density.isRefetching} style={[styles.headerButton, { backgroundColor: isDark ? "rgba(124,58,237,0.30)" : "#F3E8FF" }]} accessibilityRole="button" accessibilityLabel={language === "vi" ? "Làm mới" : "Refresh"}>{density.isRefetching ? <ActivityIndicator color="#7C3AED" /> : <RefreshCw size={19} color="#7C3AED" />}</TouchableOpacity><TouchableOpacity onPress={() => setFilterOpen(true)} style={[styles.headerButton, { backgroundColor: isDark ? "rgba(124,58,237,0.30)" : "#F3E8FF" }]} accessibilityRole="button" accessibilityLabel={copy.filter}><Filter size={19} color="#7C3AED" /></TouchableOpacity></View>
    <View style={[styles.modeRow, { backgroundColor: colors.surface, borderBottomColor: colors.border }]}>{modeOptions.map((option) => <TouchableOpacity key={option.id} onPress={() => setMode(option.id)} accessibilityRole="button" accessibilityState={{ selected: option.id === mode }} style={[styles.modeButton, option.id === mode && { backgroundColor: isDark ? "rgba(124,58,237,0.30)" : "#F3E8FF" }]}><Layers3 size={15} color={option.id === mode ? "#7C3AED" : colors.textMuted} /><Text style={[styles.modeText, { color: option.id === mode ? "#7C3AED" : colors.textMuted }]}>{option.label}</Text></TouchableOpacity>)}</View>
    <View style={styles.mapWrap}>
      <MapView style={styles.map} initialRegion={initialRegion} onPress={(event) => { setSelected(null); setDrilldownCenter(event.nativeEvent.coordinate); }}>
        {showDensity && Platform.OS === "android" ? <Heatmap points={points.map((point) => ({ latitude: point.lat, longitude: point.lng, weight: point.weight }))} radius={30} opacity={0.72} gradient={{ colors: ["#60A5FA", "#FACC15", "#F97316", "#DC2626"], startPoints: [0.1, 0.38, 0.68, 1], colorMapSize: 128 }} /> : null}
        {showDensity && Platform.OS !== "android" ? points.map((point) => <Circle key={`density-${point.incidentId}`} center={{ latitude: point.lat, longitude: point.lng }} radius={point.weight > 1 ? 310 : 230} fillColor="rgba(220,38,38,0.16)" strokeColor="rgba(249,115,22,0.36)" />) : null}
        {showPoints ? points.map((point) => <Marker key={point.incidentId} coordinate={{ latitude: point.lat, longitude: point.lng }} pinColor={severityColor(point)} onPress={() => selectPoint(point)}><Callout onPress={() => selectPoint(point)}><View style={styles.callout}><Text style={styles.calloutTitle} numberOfLines={1}>{point.title || point.incidentId}</Text><Text style={styles.calloutMeta}>{point.category || "UNCLASSIFIED"} · {point.status || "PENDING"}</Text></View></Callout></Marker>) : null}
      </MapView>
      {density.isLoading ? <View style={[styles.overlayState, { backgroundColor: `${colors.background}E8` }]}><ActivityIndicator color="#7C3AED" /><Text style={[styles.overlayText, { color: colors.textMuted }]}>{copy.loading}</Text></View> : points.length === 0 ? <View style={[styles.overlayState, { backgroundColor: `${colors.background}E8` }]}><AlertTriangle size={35} color={colors.textMuted} /><Text style={[styles.overlayText, { color: colors.text }]}>{density.isError ? copy.loading : copy.noPoints}</Text></View> : null}
      {showDensity && Platform.OS !== "android" ? <View style={[styles.infoPill, { backgroundColor: colors.surface, borderColor: colors.border }]}><Text style={[styles.infoText, { color: colors.textMuted }]}>{copy.unsupported}</Text></View> : null}
    </View>
    <View style={[styles.summary, { backgroundColor: colors.surface, borderTopColor: colors.border }]}><View style={styles.summaryItem}><Text style={[styles.summaryValue, { color: colors.text }]}>{density.data?.summary.total ?? 0}</Text><Text style={[styles.summaryLabel, { color: colors.textMuted }]}>{copy.total}</Text></View><View style={styles.summaryItem}><Text style={[styles.summaryValue, { color: "#D97706" }]}>{density.data?.summary.open ?? 0}</Text><Text style={[styles.summaryLabel, { color: colors.textMuted }]}>{copy.open}</Text></View><View style={styles.summaryItem}><Text style={[styles.summaryValue, { color: "#15803D" }]}>{density.data?.summary.resolved ?? 0}</Text><Text style={[styles.summaryLabel, { color: colors.textMuted }]}>{copy.resolved}</Text></View><View style={styles.summaryItem}><Text style={[styles.summaryValue, { color: colors.textMuted }]}>{density.data?.summary.closed ?? 0}</Text><Text style={[styles.summaryLabel, { color: colors.textMuted }]}>{copy.closed}</Text></View></View>
    <Text style={[styles.hint, { color: colors.textMuted }]}>{copy.tap}</Text>
    {(selected || drilldownCenter) ? <View style={[styles.drilldown, { backgroundColor: colors.card, borderColor: colors.border }]}><View style={styles.drilldownHeader}><View><Text style={[styles.drilldownTitle, { color: colors.text }]}>{selected?.title || copy.nearby}</Text><Text style={[styles.drilldownSubtitle, { color: colors.textMuted }]}>{drilldown.isLoading ? copy.loading : `${drilldown.data?.summary.total ?? 0} ${copy.nearby.toLowerCase()}`}</Text></View><TouchableOpacity onPress={() => { setSelected(null); setDrilldownCenter(null); }} accessibilityRole="button" accessibilityLabel={language === "vi" ? "Đóng" : "Close"}><X size={20} color={colors.textMuted} /></TouchableOpacity></View>{drilldown.data?.incidents.slice(0, 3).map((item) => <TouchableOpacity key={item.incidentId} onPress={() => navigation.getParent?.()?.navigate("AlertDetail", { id: item.incidentId })} style={[styles.nearbyItem, { borderTopColor: colors.border }]}><View style={styles.nearbyCopy}><Text style={[styles.nearbyTitle, { color: colors.text }]} numberOfLines={1}>{item.title || item.incidentId}</Text><Text style={[styles.nearbyMeta, { color: colors.textMuted }]} numberOfLines={1}>{item.address || copy.location}</Text></View><Badge label={(item.status || "PENDING").toString().replaceAll("_", " ")} type="custom" bgColor={statusColor(item)} textColor={colors.text} /></TouchableOpacity>)}</View> : null}
    <Modal visible={filterOpen} transparent animationType="slide" onRequestClose={() => setFilterOpen(false)}><View style={styles.modalOverlay}><View style={[styles.filterSheet, { backgroundColor: colors.surface }]}><View style={styles.sheetHeader}><Text style={[styles.sheetTitle, { color: colors.text }]}>{copy.filters}</Text><TouchableOpacity onPress={() => setFilterOpen(false)} accessibilityRole="button" accessibilityLabel={language === "vi" ? "Đóng" : "Close"}><X size={22} color={colors.textMuted} /></TouchableOpacity></View><ScrollView showsVerticalScrollIndicator={false}><Text style={[styles.sheetLabel, { color: colors.text }]}>{copy.status}</Text><View style={styles.options}>{STATUS_OPTIONS.map((item) => choose(item, status, setStatus))}</View><Text style={[styles.sheetLabel, { color: colors.text }]}>{copy.severity}</Text><View style={styles.options}>{SEVERITY_OPTIONS.map((item) => choose(item, severity, setSeverity))}</View><Text style={[styles.sheetLabel, { color: colors.text }]}>{copy.category}</Text><View style={styles.options}>{["ALL", ...(categories.data ?? []).filter((item) => item.isActive).map((item) => item.code)].map((item) => choose(item, category, setCategory))}</View><Text style={[styles.sheetLabel, { color: colors.text }]}>{copy.date}</Text><View style={styles.options}>{([{ id: "ALL", label: copy.all }, { id: "7D", label: copy.recent7 }, { id: "30D", label: copy.recent30 }] as const).map((item) => <TouchableOpacity key={item.id} onPress={() => setRange(item.id)} style={[styles.option, { borderColor: range === item.id ? "#7C3AED" : colors.border, backgroundColor: range === item.id ? (isDark ? "rgba(124,58,237,0.30)" : "#F3E8FF") : colors.surface }]}><Text style={{ color: range === item.id ? "#7C3AED" : colors.textMuted, fontWeight: "800", fontSize: 12 }}>{item.label}</Text>{range === item.id ? <Check size={15} color="#7C3AED" /> : null}</TouchableOpacity>)}</View></ScrollView><View style={styles.sheetActions}><TouchableOpacity onPress={resetFilters} style={[styles.clearButton, { borderColor: colors.border }]}><Text style={{ color: colors.textMuted, fontWeight: "800" }}>{copy.clear}</Text></TouchableOpacity><TouchableOpacity onPress={() => setFilterOpen(false)} style={styles.applyButton}><Text style={styles.applyText}>{copy.apply}</Text></TouchableOpacity></View></View></View></Modal>
  </View>;
};

const styles = StyleSheet.create({ container: { flex: 1 }, header: { minHeight: 72, flexDirection: "row", alignItems: "center", gap: 9, paddingHorizontal: 16, borderBottomWidth: StyleSheet.hairlineWidth }, headerCopy: { flex: 1 }, title: { fontSize: 20, fontWeight: "800" }, subtitle: { marginTop: 2, fontSize: 11 }, headerButton: { width: 40, height: 40, borderRadius: 13, alignItems: "center", justifyContent: "center" }, modeRow: { flexDirection: "row", gap: 6, paddingHorizontal: 16, paddingVertical: 9, borderBottomWidth: StyleSheet.hairlineWidth }, modeButton: { flex: 1, minHeight: 38, borderRadius: 12, flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 5 }, modeText: { fontSize: 11, fontWeight: "800" }, mapWrap: { flex: 1, minHeight: 260 }, map: { flex: 1 }, overlayState: { ...StyleSheet.absoluteFillObject, alignItems: "center", justifyContent: "center", gap: 10, paddingHorizontal: 36 }, overlayText: { textAlign: "center", fontSize: 13, fontWeight: "700" }, infoPill: { position: "absolute", left: 12, right: 12, bottom: 10, padding: 9, borderRadius: 11, borderWidth: 1 }, infoText: { textAlign: "center", fontSize: 10, fontWeight: "600" }, callout: { width: 180, padding: 4 }, calloutTitle: { fontSize: 13, fontWeight: "800" }, calloutMeta: { fontSize: 11, marginTop: 3 }, summary: { minHeight: 70, flexDirection: "row", borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 10 }, summaryItem: { flex: 1, alignItems: "center", justifyContent: "center" }, summaryValue: { fontSize: 17, fontWeight: "900" }, summaryLabel: { fontSize: 10, marginTop: 2, fontWeight: "600" }, hint: { fontSize: 10, textAlign: "center", paddingHorizontal: 20, paddingVertical: 6 }, drilldown: { marginHorizontal: 12, marginBottom: 10, padding: 13, borderRadius: 18, borderWidth: 1, maxHeight: 210 }, drilldownHeader: { flexDirection: "row", alignItems: "flex-start", justifyContent: "space-between", gap: 10 }, drilldownTitle: { maxWidth: 260, fontSize: 15, fontWeight: "800" }, drilldownSubtitle: { marginTop: 2, fontSize: 11 }, nearbyItem: { flexDirection: "row", alignItems: "center", gap: 9, borderTopWidth: StyleSheet.hairlineWidth, paddingVertical: 9 }, nearbyCopy: { flex: 1 }, nearbyTitle: { fontSize: 12, fontWeight: "800" }, nearbyMeta: { fontSize: 10, marginTop: 2 }, modalOverlay: { flex: 1, justifyContent: "flex-end", backgroundColor: "rgba(15,23,42,0.45)" }, filterSheet: { maxHeight: "82%", borderTopLeftRadius: 26, borderTopRightRadius: 26, padding: 20 }, sheetHeader: { flexDirection: "row", alignItems: "center", justifyContent: "space-between", marginBottom: 14 }, sheetTitle: { fontSize: 19, fontWeight: "800" }, sheetLabel: { fontSize: 13, fontWeight: "800", marginTop: 14, marginBottom: 8 }, options: { flexDirection: "row", flexWrap: "wrap", gap: 8 }, option: { minHeight: 38, borderRadius: 12, borderWidth: 1, paddingHorizontal: 10, flexDirection: "row", alignItems: "center", gap: 6 }, sheetActions: { flexDirection: "row", gap: 10, marginTop: 18 }, clearButton: { flex: 1, minHeight: 46, alignItems: "center", justifyContent: "center", borderWidth: 1, borderRadius: 14 }, applyButton: { flex: 1, minHeight: 46, alignItems: "center", justifyContent: "center", borderRadius: 14, backgroundColor: "#7C3AED" }, applyText: { color: "#FFF", fontWeight: "900" } });
