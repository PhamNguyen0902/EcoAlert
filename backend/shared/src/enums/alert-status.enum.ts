export enum AlertStatus {
  PENDING = 'pending',
  AI_ANALYZING = 'ai_analyzing',
  VERIFIED = 'verified',
  ASSIGNED = 'assigned',
  IN_PROGRESS = 'in_progress',
  RESOLVED = 'resolved',
  CLOSED = 'closed',
  REJECTED = 'rejected',
}
